# Oasiz Appium Tests

Studio synced 0 Appium test(s) for workspace 9bdf936c into this test directory.

- `index.json` lists every synced test case.
- `studio/<test-case>/case.json` contains the Studio test-case metadata.
- `studio/<test-case>/appium.json` contains the generated Appium commands when available.
- `studio/<test-case>/diagram.mmd` contains the Mermaid workflow diagram when available.
- `studio/<test-case>/launch-manifest.json` and `replay.json` are included when the case has them.
- `blocks/<block-id>.json` contains reusable flow blocks when Studio exports them.

Treat these files as generated from Studio. Edit or add new handcrafted tests in
a separate subdirectory if you need to preserve manual work.
