{
  "name": "oasiz-tooling",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "developers:build": "npm run build --workspace @oasiz/developer-site",
    "developers:dev": "npm run dev --workspace @oasiz/developer-site",
    "developers:edge:dev": "npm run edge:dev --workspace @oasiz/developer-site",
    "developers:edge:deploy": "npm run deploy --workspace @oasiz/developer-site",
    "upload:unity-sdk": "bun run scripts/upload-unity-sdk.ts"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.665.0",
    "dotenv": "^16.4.5"
  }
}
