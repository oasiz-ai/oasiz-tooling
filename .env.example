# Required for local npm publish or local semantic-release runs.
# One automation token can be reused for both @oasiz/sdk and @oasiz/cli
# as long as the underlying npm user has publish rights to both packages.
NPM_TOKEN=

# Optional for local semantic-release runs.
# GitHub Actions provides GITHUB_TOKEN automatically in CI.
GITHUB_TOKEN=
